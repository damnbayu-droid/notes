<!-- BEGIN:nextjs-agent-rules -->
# Technical Orientation (v18.1.7-PRODUCTION-SOVEREIGN)

This project is in a **Neural-Sovereign** state, optimized for **Biometric Identity**, **Multi-Node Registry**, and **Professional Document Engineering**.

### 🧬 Architectural Mandates
1. **Hybrid Biometric Sovereignty (v18.1.7)**: All sensitive modules (Spy Master, Settings) MUST support a dual-auth bridge: (1) Hardware-native handshakes (FaceID/Fingerprint) and (2) Neural 4-digit PIN fallback. Never force a blocking prompt.
2. **Device Node Registry (v18.1.7)**: All active nodes MUST register their metadata (OS, browser, note count) via the `user_devices` registry. The Settings Hub MUST provide real-time visibility into the multi-device intelligence cluster.
3. **Professional PDF Engineering (v18.1.7)**: The PDF Master MUST preserve original document scaling and metadata during all operations (Merge, Split, Compress, Image-to-PDF). Annotations must be surgical and non-destructive.
4. **Storage Resilience (Protocol QUOTA)**: All localStorage interactions MUST route through the `safeSetItem` utility. Tiered pruning MUST be enforced: (1) Recovery Buffer -> (2) Legacy Device Segments.
5. **Structural Discovery Protocol (v7.6.2)**: All shared intelligence MUST be delivered in a semantically valid DOM tree for 100% parsing success for non-visual AI agents.
6. **Privacy-First Sitemap**: The `sitemap.xml` MUST EXCLUSIVELY index nodes where `is_discoverable = true`.
7. **Fast Open SSR Architecture**: All root routing shells MUST implement instantaneous SSR layout mapping via `DashboardSkeleton.tsx`.
8. **Data Integrity Mandate (Protocol BUFF)**: All intelligence editing interfaces MUST implement ultra-redundant persistence: Force-Save on Exit and Black-Box local recovery.
9. **Neural Auth Bus (Singleton Protocol)**: All user data MUST route through the unified `initializeAuth` memory pool within `hooks/useAuth.ts`.
10. **Neural Media Protocol (v7.7.0)**: All intelligence nodes MUST prioritize hosted assets (WebP/AVIF) over embedded data.
11. **Shared Backend**: Connected to Supabase project `dfxhfutflhnxjjpbqscj`. Data integrity and RLS policies are strictly enforced.

### 📚 Relevant Documentation
- `README.md`: General overview.
- `NEURAL_SYSTEM_AUDIT.md`: Latest v18.1.7 stability metrics and build reports.
- `walkthrough.md`: History of the hardening phase and biometric refinements.
- `FEATURE.md`: Full manifest of operational capabilities.
- `COMPREHENSIVE_REPORT.md`: Strategic intelligence summary.
<!-- END:nextjs-agent-rules -->
